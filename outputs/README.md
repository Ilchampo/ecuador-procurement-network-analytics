# Outputs

Publication figures are stored in `figures/`. Generated analytical tables belong in `tables/`. Every output is produced by a numbered notebook and should be treated as reproducible rather than manually edited.
